{
    "name": "pump-iot-web-monorepo",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "dev": "pnpm -r --parallel dev",
        "build": "pnpm -r build",
        "lint": "pnpm -r lint"
    },
    "devDependencies": {
        "turbo": "^2.3.3"
    }
}